import React from 'react';
export default class StudentRosterHeader extends React.Component<any, any> {
    constructor(props: any);
    handleAddClicked(): void;
    handleSelectChanged(e: any): void;
    renderSelect(otherStudents: any): React.JSX.Element | "If a student already has an account, ask the student to enter the Class Word above";
    render(): React.JSX.Element;
}
