-# This is using the minimal layout, so we need to add the header ourselves
= render :partial => 'shared/project_header',
  :locals => { :extra_options => { :isWaysToGive => true } }

.cols
  .col-12
    .padded_content
      = custom_content.html_safe
