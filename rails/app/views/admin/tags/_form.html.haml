.item
  = edit_menu_for(admin_tag, f, { :omit_cancel => true })
  - if admin_tag.errors.any?
    = admin_tag.errors.map(&:full_message).join(', ')
  .content
    %p
      %ul.menu_v
        %li
          Scope:
          =f.text_field :scope

        %li
          Tag:
          =f.text_field :tag


