export { IPermissionForm, IProject, CurrentSelectedProject } from "../common/types";

import { IPermissionForm } from "../common/types";

export type IPermissionFormFormData = Partial<IPermissionForm>;
