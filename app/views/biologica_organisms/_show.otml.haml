%OTText{ :local_id => ot_local_id_for( biologica_organism, :name ), :rows => '2', :columns => '40' }
  %text= biologica_organism.name
%OTCompoundDoc{:local_id => ot_local_id_for(biologica_organism)}
  %bodyText
    = biologica_organism.description  
    %object{ :refid => ot_refid_for(biologica_organism, :name) }
