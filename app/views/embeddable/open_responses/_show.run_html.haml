%div{:id => dom_id_for(open_response, :details), :class => 'content'}
  = open_response.prompt

  %textarea{:type => 'text', :style=>"width: 100%;", :value =>open_response.default_response, :class=> "mceNoEditor",:id => dom_id_for(open_response, :item, :default_response_field)}
