= form_for(@biologica_multiple_organism, :before => "tinyMCE.triggerSave(true,true)") do |f|
  .item
    = render :partial => 'form', :locals => { :biologica_multiple_organism => @biologica_multiple_organism, :f => f }
