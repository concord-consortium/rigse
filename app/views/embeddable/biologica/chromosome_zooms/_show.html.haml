%div{:id => dom_id_for(chromosome_zoom, :details), :class => 'content'}
  = wrap_edit_link_around_content(chromosome_zoom, options={:complete => mce_init_string}) do
    = chromosome_zoom.description
