%div{:id => dom_id_for(biologica_chromosome, :details), :class => 'content'}
  = wrap_edit_link_around_content(biologica_chromosome, options={:complete => mce_init_string}) do
    = biologica_chromosome.description.html_safe
