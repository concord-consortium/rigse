- remote_form_for(biologica_world, :update => dom_id_for(biologica_world, :item), :before =>  "tinyMCE.triggerSave(true,true)") do |f|
  = render :partial => 'form', :locals => { :biologica_world => biologica_world, :f => f  }