%div{:id => dom_id_for(choice, :item), :style => 'display: table-cell; text-align: center;'}
  %i= "#{choice.choice}"
  %br
  %input{:type=>(multiple_choice.allow_multiple_selection ? 'checkbox' : 'radio'), :value=>"#{dom_id_for(choice)}", :name => "questions[#{dom_id_for(multiple_choice)}][]", :checked => (local_assigns[:selected] ? true : false)}
