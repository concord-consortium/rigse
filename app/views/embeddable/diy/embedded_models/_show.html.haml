%div{:id => dom_id_for(embedded_model, :details), :class => 'content'}
  = wrap_edit_link_around_content(embedded_model, options={:complete => mce_init_string}) do
    %ul

      %li
        Authored Data URL: 
        %a{:href => embedded_model.url}
          = embedded_model.url
