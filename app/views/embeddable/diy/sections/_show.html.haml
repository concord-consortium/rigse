%div{:id => dom_id_for(diy_section, :details), :class => 'content'}
  = #wrap_edit_link_around_content(section, options={:complete => mce_init_string}) do
  = diy_section.content
  - if diy_section.has_question
    %textarea{:type => 'text', :style=>"width: 95%;", :value =>diy_section.default_response, :class=> "mceNoEditor",:id => dom_id_for(diy_section, :item, :default_response_field)}
