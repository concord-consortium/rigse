%OTQuestion{:local_id => ot_local_id_for(section)}
  %prompt
    %OTCompoundDoc
      %bodyText= section.prompt
  %input
    %OTCompoundDoc
      %bodyText
        %table{:width => '90%'}
          %tr
            %td{:width => '5%'}
            %td
              %object{:refid => ot_refid_for(section, :response)}

%OTText{ :local_id => ot_local_id_for(section, :response), :rows => '5', :columns => '35', :fontSize => '12', :name => "#{section.name}_field"}
  %text= h(section.default_response)
