%div{:id => dom_id_for(section, :details), :class => 'content'}
  = section.prompt

  %textarea{:type => 'text', :style=>"width: 100%;", :value =>section.default_response, :class=> "mceNoEditor",:id => dom_id_for(section, :item, :default_response_field)}
