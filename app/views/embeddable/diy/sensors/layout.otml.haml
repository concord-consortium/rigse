!!! XML
= render :partial => "otml/character_entities"
%otrunk{ :id => @sensor.uuid}
  =ot_imports
  %objects
    %OTSystem
      =ot_bundles(:title => h(@sensor.name))
      %root
        %OTCompoundDoc
          %bodyText
            #content
              %h3
                = h(Embeddable::NLogoModel.display_name + ': ' + @sensor.name)
              %object{ :refid => ot_refid_for(@sensor) }
      %library
        =yield :layout
