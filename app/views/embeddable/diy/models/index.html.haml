= render :partial => 'shared/collection_menu', :locals => { :collection => @models, :collection_class => Embeddable::NLogoModel }        
- @models.each do |model|
  = render :partial => 'shared/embeddable_container', :locals=> {:component => model}
