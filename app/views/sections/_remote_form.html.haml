- remote_form_for(section, :update => dom_id_for(section, :item), :before =>  "tinyMCE.triggerSave(true,true)") do |f|
  = edit_menu_for(section, f)
  = f.hidden_field :investigation_id, :value =>investigation.id
  - field_set_tag 'nanna' do
    = f.text_field :name, :id => dom_id_for(section, :modal, :name_field)
  - field_set_tag 'description' do
    = f.text_area :description, :id => dom_id_for(section, :modal, :textarea), :class => 'tinymce_textarea'

  