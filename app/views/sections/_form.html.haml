- form_for(@section) do |f|
  %ul.menu
    %li.menu= "#{@section.name}:"
    %li.menu= f.submit "Save"
  - field_set_tag 'Section Name' do
    = f.text_field :name

  - field_set_tag 'Section Description' do
    = f.text_area :description, :cols => 80, :rows => 5, :class => 'tinymce_textarea'
