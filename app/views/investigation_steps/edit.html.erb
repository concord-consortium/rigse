<h1>Editing investigation_steps</h1>

<% form_for(@investigation_steps) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @investigation_steps %> |
<%= link_to 'Back', investigation_steps_path %>
