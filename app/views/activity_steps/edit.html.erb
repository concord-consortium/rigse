<h1>Editing activity_steps</h1>

<% form_for(@activity_steps) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @activity_steps %> |
<%= link_to 'Back', activity_steps_path %>
