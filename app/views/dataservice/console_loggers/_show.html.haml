- options = { :complete => mce_init_string, :omit_delete => true }
%div{ :id => dom_id_for(dataservice_console_logger), :class => "container_element" }
  = show_menu_for(dataservice_console_logger, options)

  %div{:id => dom_id_for(dataservice_console_logger, :item), :class => 'item'}
    %div{:id => dom_id_for(dataservice_console_logger, :details), :class => 'content'}
      = wrap_edit_link_around_content(dataservice_console_logger, options={:complete => mce_init_string}) do
        %h4= h(dataservice_console_logger.name)
        %p
          %ul.menu_v
    
