- options = { :complete => mce_init_string, :omit_delete => true }
%div{ :id => dom_id_for(dataservice_bundle_logger), :class => "container_element" }
  = show_menu_for(dataservice_bundle_logger, options)

  %div{:id => dom_id_for(dataservice_bundle_logger, :item), :class => 'item'}
    %div{:id => dom_id_for(dataservice_bundle_logger, :details), :class => 'content'}
      = wrap_edit_link_around_content(dataservice_bundle_logger, options={:complete => mce_init_string}) do
        %h4= h(dataservice_bundle_logger.name)
        %p
          %ul.menu_v
    
