%OTText{ :local_id => ot_local_id_for( n_logo_model, :name ), :rows => '2', :columns => '40' }
  %text= n_logo_model.name
%OTCompoundDoc{:local_id => ot_local_id_for(n_logo_model)}
  %bodyText
    = n_logo_model.description  
    %object{ :refid => ot_refid_for(n_logo_model, :name) }
