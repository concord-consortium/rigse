%h1 Recent Activity
= render :partial => "notice"
#recent-activity

:javascript
  PortalPages.renderRecentActivity({dataUrl: Portal.API_V1.OFFERINGS + '?user_id=#{current_user.id}'}, 'recent-activity');
