#main
  %h3= "#{APP_CONFIG[:site_name]}: Technical Help"
  .padded_content
    %p
      %strong= "You need to run the RITES installer for #{@os} before running an investigations"
      %p (TODO: link to the installer dynamically)
      %p=link_to('download the osx installer', "/installers/RITES-#{jnlp_installer_version}-osx-installer.dmg")