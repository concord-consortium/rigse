- content_for :top_menu_items do
  - add_top_menu_item(link_to('Investigations', investigations_path))

- content_for(:side_bar) do
  %p
    - field_set_tag 'Guest' do
      Hello Guest

- field_set_tag "You're not logged in!" do
  %p= render :partial => 'sessions/new'
    
- render_project_info
= yield :project_info
