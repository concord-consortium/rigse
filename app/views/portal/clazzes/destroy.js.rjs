container_dom_id = dom_id_for(@portal_clazz, :accordion_container)
safe_js(page,container_dom_id) do
  page.remove container_dom_id
end
item_dom_id = dom_id_for(@portal_clazz, :accordion_item)
safe_js(page,item_dom_id) do
  page.remove item_dom_id
end
