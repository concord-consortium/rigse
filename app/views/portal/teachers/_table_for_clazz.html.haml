/ - student_list = (students_in_class(portal_clazz.students)).sort { |a,b| (a.user.last_name <=> b.user.last_name) }
%table{:width=>"100%"}
  %th
    Teacher name
  %th
    Teacher login
  %th
    action
  - current_teachers.each do |portal_teacher|
    / - portal_student_clazz = portal_clazz.student_clazzes.find_by_student_id(portal_student.id)
    %tr{:id=>dom_id_for(portal_teacher)}
      %td
        = link_to portal_teacher.name, portal_teacher
      %td
        = portal_teacher.login
      %td
        - if portal_clazz.changeable?(current_user)
          = remote_link_button "delete.png", :url => {:action=>'remove_teacher', :id => portal_clazz}, :confirm => "This action will remove the teacher: '#{portal_teacher.name}' from the class: #{portal_clazz.name}. \nAre you sure you want to do this?", :method => :delete, :title => "remove #{portal_teacher.name} from #{portal_clazz.name}"
