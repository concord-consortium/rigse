<h1>Listing teachers</h1>

<table>
  <tr>
  </tr>

<% @portal_teachers.each do |teacher| %>
  <tr>
    <td><%= link_to 'Show', teacher %></td>
    <td><%= link_to 'Edit', edit_portal_teacher_path(teacher) %></td>
    <td><%= link_to 'Destroy', teacher, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New teacher', new_portal_teacher_path %>