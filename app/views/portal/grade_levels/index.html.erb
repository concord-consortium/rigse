<h1>Listing grade_levels</h1>

<table>
  <tr>
  </tr>

<% @grade_levels.each do |grade_level| %>
  <tr>
    <td><%= link_to 'Show', grade_level %></td>
    <td><%= link_to 'Edit', edit_portal_grade_level_path(grade_level) %></td>
    <td><%= link_to 'Destroy', grade_level, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New grade_level', new_portal_grade_level_path %>