<h1>Listing offerings</h1>

<table>
  <tr>
  </tr>

<% @portal_offerings.each do |offering| %>
  <tr>
    <td><%= link_to 'Show', offering %></td>
    <td><%= link_to 'Edit', edit_portal_offering_path(offering) %></td>
    <td><%= link_to 'Destroy', offering, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New offering', new_portal_offering_path %>