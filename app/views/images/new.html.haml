%h1
  Create image

= error_messages_for :image
- form_for(:image, :url => images_path, :html => { :multipart => true }) do |f|
  = render :partial => 'form', :locals => {:f => f}
  %p
    = submit_tag "Create"

= link_to 'List', images_path
