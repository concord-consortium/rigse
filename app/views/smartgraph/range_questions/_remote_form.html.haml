- remote_form_for(range_question, :update => dom_id_for(range_question, :item), :before =>  "tinyMCE.triggerSave(true,true)") do |f|
  = render :partial => "smartgraph/range_questions/form", :locals => { :range_question => range_question, :f => f  }