%h1 new facilitator note

- form_for(@teacher_note) do |f|
  = f.error_messages
  = render :partial => "form", :locals => {:f => f, :teacher_note => @teacher_note}

= link_to 'Back', teacher_notes_path