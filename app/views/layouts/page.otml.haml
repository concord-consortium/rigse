!!! XML
%otrunk{ :id => @page.uuid }
  =ot_imports
  %objects
    %OTSystem
      =ot_bundles(:title => @page.name)
      %root
        =yield "page_#{dom_id_for(@page)}".to_sym
      %library
        =yield :library