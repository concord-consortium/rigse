!!! XML
= render :partial => "otml/character_entities"
%otrunk{ :id => @section.uuid }
  =ot_imports
  %objects
    %OTSystem
      =ot_bundles(:title => h(@section.name))
      %root
        =yield "section_#{dom_id_for(@section)}".to_sym
      %library
        =yield :library
