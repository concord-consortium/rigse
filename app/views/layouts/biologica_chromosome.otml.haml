!!! XML
= render :partial => "otml/character_entities"
%otrunk{ :id => @biologica_chromosome.uuid}
  =ot_imports
  %objects
    %OTSystem
      =ot_bundles(:title => @biologica_chromosome.name)
      %root
        %OTCompoundDoc
          %bodyText
            #content
              %h3
                = BiologicaChromosome.display_name + ': ' + @biologica_chromosome.name
              %object{ :refid => ot_refid_for(@biologica_chromosome) }
      %library
        =yield :layout