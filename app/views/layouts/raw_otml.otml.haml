!!! XML
= render :partial => "otml/character_entities"
%otrunk{ :id => @raw_otml.uuid}
  =ot_imports
  %objects
    %OTSystem
      =ot_bundles(:title => @raw_otml.name, :authoring => @authoring)
      %root
        = @raw_otml.otml_content
      %library
        =yield :layout