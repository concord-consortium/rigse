!!! XML
= render :partial => "otml/character_entities"
%otrunk{ :id => @biologica_world.uuid}
  =ot_imports
  %objects
    %OTSystem{:local_id => 'system'}
      =ot_bundles(:title => h(@biologica_world.name))
      %root
        %OTCompoundDoc
          %bodyText
            #content
              %h3
                = h(Embeddable::Biologica::World.display_name + ': ' + @biologica_world.name)
              %object{ :refid => ot_refid_for(@biologica_world) }
      %library
        = content_for :layout