!!!
%head
  %meta{:charset => "utf-8"}/
  %title
    #{site_name} Maintenance
  :css
    /**/
      body{
        background:#fee9aa;
        color:#3a878b;
        font:100% 'lucida grande', 'trebuchet ms', arial, sans-serif;
        line-height:150%;
      }
      #container{
        background:url(maintenance/bubble.png) no-repeat;
        height:395px;
        left:50%;
        margin:-187px 0 0 -300px;
        position:absolute;
        top:50%;
        width:600px;
      }
      #container p{
        margin:30px 70px 0 100px;
      }
      #cc-link {
        display:block;
        margin:55px 80px 0 0;
        text-align:right;
      }
      a {
        color:#eb8723;
      }
    /**/
    %body
  #container
    %p
      As of #{Time.now.strftime("%b %d,  %I:%M%p (%Z)")}
      #{site_name} is undergoing maintenance. Sorry for the inconvenience.
      We'll be back  #{back_up} .
    %p
      Please
      %a{:href => "mailto:rites-help@concord.org"}
        email us
      if you need to get in touch.
    %p
      #{message}
