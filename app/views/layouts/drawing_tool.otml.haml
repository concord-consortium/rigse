!!! XML
%otrunk{:id => @content_for_otrunk_doc_id}
  =ot_imports
  %objects
    %OTSystem
      =ot_bundles(:title => @drawing_tool.name)
      %root
        %OTCompoundDoc
          %bodyText
            #content
              %h1 
                Basic Drawing Tool
              %p
                This is a rendering of a drawing tool object
              %object{ :refid => @content_for_drawing_tool_object_id }/
      %library
        =yield :layout