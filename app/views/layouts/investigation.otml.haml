!!! XML
%otrunk{ :id => @investigation.uuid }
  =ot_imports
  %objects
    %OTSystem
      =ot_bundles(:left_nav_panel => true)
      %root
        =yield :investigation
      %library
        =yield :library
