- @user.password = @user.password_confirmation = nil
= title "Signup", :h2
- form_for @user do |form|
  = form.error_messages
  = render :partial => 'user', :locals => { :f => form }
  = render :partial => 'password', :locals => { :f => form }
  .buttons
    = submit_tag 'Sign up'
