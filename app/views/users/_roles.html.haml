= hidden_field(:user, :has_roles_in_form, :value => true)
%fieldset
  %legend
    Roles
  %ul.menu_h
    - roles.each do |r|
      %li
        = r.title.capitalize
        = check_box_tag "user[role_ids][]", r.id, @user.roles.include?(r)
