%li#home.trail= link_to 'Home', home_path
%li.trail= link_to 'About', about_path
- if current_user.has_role?('admin', 'manager', 'researcher', 'author', 'member') || current_user.portal_teacher
  %li.trail=link_to(top_level_container_name.humanize.pluralize, activities_path)
- if current_user.has_role?('admin', 'manager', 'researcher', 'author') || current_user.portal_teacher
  %li.trail=link_to "#{Diy::Model.display_name.pluralize}", diy_models_path
-#- if current_user.has_role?('admin') || current_user.portal_teacher
  -#%li.trail= link_to "#{ResourcePage.display_name.pluralize}", resource_pages_path
- if current_user.has_role?('guest')
  %li.trail= link_to "Activity Previews", activities_path
- if current_user.has_role?('admin', 'manager', 'researcher', 'author', 'member') || current_user.portal_teacher
  %li.trail=link_to 'CareerSight', 'http://careersight.concord.org', :target => '_blank'
  %li.trail=link_to 'ProbeSight', 'http://probesight.concord.org', :target => '_blank'
